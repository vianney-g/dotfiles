# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

exec --no-startup-id "xrandr --output DP-3 --auto"
exec --no-startup-id "xrandr --output eDP-1 --auto"

# Screenshot
exec --no-startup-id flameshot

# activate numlock
exec --no-startup-id xdotool key --clearmodifiers Num_Lock

# background image (needs feh)
exec --no-startup-id feh --bg-fill ~/Images/Papiers_peints/pexels-jplenio-1435075.jpg

# Set keyboard layout to French Bépo or French Azerty
# bepo must be set as variant first to avoid a bug with the keymap
# otherwise, shortcuts with the $meta key won't work
exec "setxkbmap -option 'grp:lctrl_lwin_toggle' -layout fr,fr -variant 'bepo,azerty'"


# Readable modifier key names.
set $meta Mod4
set $alt Mod1
set $hyper Mod4+Mod1+control+shift
set $meh Mod1+control+shift

# Reset xrandr settings
bindsym $meta+Shift+y exec --no-startup-id xrandr --output eDP-1 --auto

# Font for window titles.
# font pango:monospace 8
font pango:JetBrainsMono Nerd Font 11
default_border pixel 3
hide_edge_borders smart

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex --autostart --environment i3

focus_on_window_activation focus
for_window [urgent="latest"] focus

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.


# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet


# Adjust brightness
bindsym XF86MonBrightnessDown exec --no-startup-id brightnessctl --min-val=2 -q set 3%-
bindsym XF86MonBrightnessUp exec --no-startup-id brightnessctl -q set 3%+

# Adjust volume
bindsym XF86AudioRaiseVolume exec --no-startup-id amixer -q set Master 2%+
bindsym XF86AudioLowerVolume exec --no-startup-id amixer -q set Master 2%-
bindsym XF86AudioMute exec --no-startup-id amixer -q set Master toggle

# toggle playerctl
bindsym XF86AudioPlay exec --no-startup-id playerctl play-pause

# Use Mouse+$meta to drag floating windows to their wanted position
floating_modifier $meta

# start a terminal
# bindsym $meta+Return exec i3-sensible-terminal
bindsym $meta+Return exec --no-startup-id wezterm

# spawn snippets selector
bindsym $meta+comma exec --no-startup-id snippets

# take a screenshot
bindsym $meta+Delete exec --no-startup-id flameshot gui

# kill focused window
bindsym $meta+period kill

# start clipboard manager
# bindsym $hyper+c exec --no-startup-id diodon
bindsym $meta+c exec --no-startup-id "rofi-gpaste"

# start dmenu (a program launcher)
# bindsym $meta+d exec --no-startup-id dmenu_run

# A more modern dmenu replacement is rofi:
bindsym $meta+d exec --no-startup-id "rofi -show run"
# switch active window with rofi
bindsym $meta+l exec --no-startup-id "rofi -show window"
# There also is i3-dmenu-desktop which only displays applications shipping a
# .desktop file. It is a wrapper around dmenu, so you need that installed.
# bindcode $meta+40 exec --no-startup-id i3-dmenu-desktop

# name some task related workspaces
set $Web " Web"
set $Code " Code"
set $Game " Jeu"
set $Private " Privé"
set $Social " Social"
set $Music " Musique"
set $Misc " Autre"
set $Doc " Doc"
set $Photo " Photo"
set $Video " Vidéo"
set $Mail " Mail"
set $Read " Lecture"
set $Home " Home"
set $Help "Aide"
set $Kanban " Kanban"
set $Legitchad "Légitchad"
set $Task " Tâches"
set $Log " Log"
set $Note " Note"
set $Project " Projet"
set $Search " Recherche"
set $Shop " Shop"
set $Update " MàJ"
set $Urgent " Urgent"
set $Videoconf " Visio"
set $Vim " Vim"

# change focus
bindsym $meta+u focus left
bindsym $meta+i focus down
bindsym $meta+p focus up
bindsym $meta+e focus right

# move focused window
bindsym $hyper+u move left
bindsym $hyper+i move down
bindsym $hyper+p move up
bindsym $hyper+e move right

# move focused container to workspace
bindsym $meh+u move container to workspace prev workspace prev
bindsym $meh+i move container to workspace prev workspace prev
bindsym $meh+p move container to workspace next workspace next
bindsym $meh+e move container to workspace next workspace next
bindsym $meh+f move container to workspace $Web workspace $Web
bindsym $meh+c move container to workspace $Code workspace $Code
bindsym $meh+g move container to workspace $Game workspace $Game
bindsym $meh+x move container to workspace $Private workspace $Private
bindsym $meh+s move container to workspace $Social workspace $Social
bindsym $meh+m move container to workspace $Music workspace $Music

# move focused window to scratchpad
bindsym $meh+r move scratchpad

#display scratchpad
bindsym $meh+n scratchpad show

# split in horizontal orientation
bindsym $meta+h split h

# split in vertical orientation
bindsym $meta+v split v

# enter fullscreen mode for the focused container
bindsym $meta+y fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $meta+s layout stacking
bindsym $meta+z layout tabbed
bindsym $meta+t layout toggle split

# toggle tiling / floating
bindsym $meta+f floating toggle

# change focus between tiling / floating windows
bindsym $meta+k focus mode_toggle

# focus the parent container
bindsym $meta+q focus parent

# focus the child container
#bindsym $meta+d focus child

# switch to workspace
bindsym $meta+Shift+u workspace prev
bindsym $meta+Shift+i workspace prev
bindsym $meta+Shift+p workspace next
bindsym $meta+Shift+e workspace next
# or, using the mouse wheel:
bindsym --whole-window $meta+Shift+button4 workspace prev
bindsym --whole-window $meta+Shift+button5 workspace next

# Dynamic workspace
bindsym $meta+Shift+n exec i3-msg workspace `i3-input | grep command | cut -c 11-30`

bindsym $meta+Shift+f workspace $Web
bindsym $meta+Shift+c workspace $Code
bindsym $meta+Shift+g workspace $Game
bindsym $meta+Shift+x workspace $Private
bindsym $meta+Shift+s workspace $Social
bindsym $meta+Shift+m workspace $Music
bindsym $meta+Shift+l exec i3-msg workspace `echo "$Web\n$Code\n$Game\n$Private\n$Social\n$Music\n$Misc\n$Doc\n$Photo\n$Video\n$Mail\n$Read\n$Home\n$Help\n$Kanban\n$Legitchad\n$Task\n$Log\n$Note\n$Project\n$Search\n$Shop\n$Update\n$Urgent\n$Videoconf\n$Vim" | rofi -dmenu -p "Switch to workspace" -i`
bindsym $meta+Shift+d exec i3-msg move container to workspace `echo "$Web\n$Code\n$Game\n$Private\n$Social\n$Music\n$Misc\n$Doc\n$Photo\n$Video\n$Mail\n$Read\n$Home\n$Help\n$Kanban\n$Legitchad\n$Task\n$Log\n$Note\n$Project\n$Search\n$Shop\n$Update\n$Urgent\n$Videoconf\n$Vim" | rofi -dmenu -p "Move to workspace" -i`
# reload the configuration file
bindsym $meta+Shift+j reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $meta+Shift+z restart

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym c resize shrink width 10 px or 10 ppt
        bindsym t resize grow height 10 px or 10 ppt
        bindsym s resize shrink height 10 px or 10 ppt
        bindsym r resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $meta+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $meta+r mode "default"
}

bindsym $meta+r mode "resize"

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

set $session_mode 🆂 suspend | 🅻 lock | 🆀 exit | 󱊷 cancel
bindsym $meta+x mode "$session_mode"
mode "$session_mode" {
    # bindsym l exec i3lock --ignore-empty-password --tiling --image='/home/vianney/Images/Papiers_peints/1270960.png' 2>/dev/null, mode "default"
    bindsym l exec betterlockscreen --lock dim --off 120 -- --keylayout=0, mode "default"

    # just force suspend. i3lock will be called automatically via lock.sh 
    bindsym s exec --no-startup-id sleep 1 && xset dpms force suspend , mode "default"
    # exit i3 (logs you out of your X session)
    bindsym q exit, mode "default"

    bindsym Escape mode "default"
    bindsym Return mode "default"
}

exec_always --no-startup-id ~/.config/i3/polybar.sh

# Some nice colors (theme tomorrow-night)
client.focused #f0c674 #f0c674 #1d1f21 #f0c674 #f0c674
client.unfocused #282a2e #282a2e #1d1f21 #282a2e #282a2e
client.urgent #cc6666 #cc6666 #1d1f21 #cc6666 #cc6666
client.background #1d1f21

# Always stick some apps to dedicated workspace
for_window [class="Spotify"] move container to workspace $Music
for_window [class="Slack"] move container to workspace $Social
for_window [class="notepad"] floating enable, sticky enable, border pixel 10, resize set 20ppt 20ppt, move position 75ppt 75ppt

# start a notepad
bindsym $meta+n exec --no-startup-id notepad
